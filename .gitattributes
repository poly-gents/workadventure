# Ensure shell scripts and Docker-related files always use LF line endings, even on Windows
# This prevents "bad interpreter" errors when running bash scripts in Docker containers

# Shell scripts
*.sh text eol=lf

# Docker and container-related files
Dockerfile* text eol=lf
*.dockerfile text eol=lf
